/**
 * GitHub Device Flow Auth – frontend only
 * (OAuth Device Authorization Grant)
 */

export async function notImplementedYet(): Promise<never> {
  throw new Error('GitHub Device Flow wiring not enabled yet')
}
